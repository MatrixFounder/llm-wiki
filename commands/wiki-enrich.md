---
description: Ingest a raw source via wiki-ingest, then index the manifest into the SQLite database
---

Read and execute the workflow defined in `workflows/wiki-enrich.md`.

Follow all steps sequentially. Apply all Global Protocols
(skill-archive-task, skill-session-state) where relevant.

User's task context:
$ARGUMENTS
