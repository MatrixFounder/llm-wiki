---
description: Import an external article/paper/X-thread into a PARA vault — fetch+convert, translate/summarize (fed known-concepts), file the note + concept pages, index
---

Read and execute the workflow defined in `workflows/wiki-import-article.md`.

Follow all steps sequentially. Apply all Global Protocols
(skill-archive-task, skill-session-state) where relevant.

User's task context:
$ARGUMENTS
