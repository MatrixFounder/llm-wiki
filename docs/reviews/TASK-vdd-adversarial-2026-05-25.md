# VDD Critique: TASK 001 — wiki-mvp (post-transcript-workflow edits)

Reviewer: Sarcasmotron (fresh context). Target: `/Users/sergey/dev-projects/obsidian-llm-wiki/docs/TASK.md` after R-06.3/R-07.4/R-07.5/I-3.3/I-3.4/UC-07/§6.1/§6.2 additions.

## 1. Executive Summary

- **Verdict**: **WARNING** (close to PASS, but with 4 HIGH-severity gaps that will produce real runtime failures or contradictions, not just doc fuzz).
- **Confidence**: High. Most issues are cross-checkable against `SCHEMA-DRAFT.sql` and the actual workflow file — they're not stylistic.
- **Summary**: The transcript-workflow integration is conceptually sound but leaks specificity at three load-bearing seams: (1) the "idempotency" claim in UC-07 A4 has no schema column to land in, (2) the subprocess invocation in I-3.3(c) names a `claude -p ...` CLI that may not exist in every host environment and has no timeout/cleanup contract, (3) file↔DB type divergence will fight `wiki-lint` R-11.1 index-drift unless lint is taught the mapping. R-07.5 regex stripping is described in prose with no anchor for the failure modes.

---

## 2. Risk Analysis

| Severity | Category | Issue | Impact | Recommendation |
|:---|:---|:---|:---|:---|
| **HIGH** | Spec-Gap / Schema | **UC-07 A4 invents a `source_hash` field that does not exist anywhere.** A4 says "compute hash of transcript-source, skip LLM if matches stored `<output>/summary.md.source_hash` (saved metadata)". No column in `pages`, no row in `vault_metadata`, no frontmatter field in the workflow's frontmatter spec, and the `<output>/summary.md.source_hash` sidecar path appears nowhere else. Idempotency is fictional as written. | Re-running `/ingest-source --kind transcript` ALWAYS re-spawns the LLM workflow → $0.20-$0.60 burned per re-run. The "AC-test for idempotency" cannot be implemented. | Pick ONE concrete location: (a) add `transcript_source_hash TEXT` to the workflow's frontmatter extension and to `pages.frontmatter_json` reads, OR (b) add a `source_state` row with `source_kind='transcript', scope=abs(source), key='source_hash'` (table already exists in SCHEMA-DRAFT.sql §8). Pin the choice in R-06.3 and UC-07 A4 with the exact SQL/YAML key. Until then, drop the idempotency AC at TASK.md:552. |
| **HIGH** | Logic / Spec-Gap | **I-3.3 step (c) subprocess contract is undefined.** `claude -p "/generate-detailed-meeting-summary on transcript … output to …"` assumes the `claude` CLI binary is on `$PATH`, accepts `-p`, treats slash-commands as positional input, and returns exit-code on workflow failure. None of this is verified in the spec. No timeout handling, no signal handling (SIGTERM mid-run leaves `<output>/summary.md` half-written), no env isolation. UC-07 A5 mentions "subprocess превышает timeout (`> 10 min`) — fail-fast" but I-3.3 doesn't say HOW (no `subprocess.TimeoutExpired`, no `Popen.kill` + cleanup). | Production: hung subprocess → orphan write of partial markdown → subsequent re-ingest reads it as legit summary because `agent-metadata` marker is checked AFTER reading the file (TASK.md:497-501), but a truncated file may still have the marker if the workflow happens to write it before crashing. | In I-3.3 add explicit step (c.1): "Spawn via `subprocess.run([...], timeout=600, capture_output=True, check=False)`. On `TimeoutExpired`: `proc.kill()`, `unlink(<output>/summary.md)` (best-effort), move any partial to `_raw/failed/<ts>-<slug>.md`, return `WORKFLOW_TIMEOUT`." Also: spec the discovery contract — "if `shutil.which('claude') is None` → `WORKFLOW_NOT_FOUND` with `expected_paths=['claude on PATH']`". Optionally support an alternative `WIKI_GENSUMMARY_CMD` env var so non-Claude-CLI environments (Cursor, IDE plugins) can plug in. |
| **HIGH** | Schema / Logic | **R-11.1 index-drift will false-positive on every transcript page.** File on disk keeps `type: lesson-summary`; DB stores `pages.type='summary'`. `wiki-lint`'s drift check (R-11.1, UC-04 step 3.6) is described as "walk filesystem, compare с pages-table" — but no rule says drift must IGNORE the mapped-type difference. As specified, lint will flag every transcript-derived page as drifted. | UC-04 AC at TASK.md:358 says "On clean vault → `✅ Healthy. No issues found.`" — that AC fails the moment you ingest a transcript. Self-contradiction between R-07.4 (intentional file↔DB divergence) and R-11.1 (drift detection). | Add explicit AC to R-11.1 and §6.1 type-mapping rule: "Drift check applies §6.1 forward-mapping before comparing `pages.type`. A page with frontmatter `type ∈ {summary-light, lesson-summary, meeting-summary}` whose `pages.type='summary'` AND whose `tags` contains the corresponding marker is NOT drift." Add a UC-04 AC: "After UC-07 (transcript ingest), `wiki-lint` reports 0 drift issues for that page." |
| **HIGH** | Security / Logic | **R-07.5 body-normalization regex is unspecified and the failure modes are not bounded.** I-3.4 says "регex-based" stripping of ` ```mermaid…``` ` and `<!-- SECTION:* -->` "перед FTS5-индексацией". No regex pattern. Three concrete failure modes: (1) malformed Mermaid block with no closing ` ``` ` → naive `(?s)```mermaid.*?```` eats body to EOF; (2) nested fences inside Mermaid (`flowchart LR\n  A["text with \`backticks\`"]`) → unclear; (3) legitimate HTML comments (`<!-- TODO: revisit -->`) get stripped because the spec says "strip `<!-- SECTION:* -->` HTML-anchors" but the regex is unspecified — a lazy author writes `<!--.*?-->` and torches every comment. | FTS5 index loses real content silently. body_excerpt computed from stripped body misses the first 500 chars. UC-07 AC at TASK.md:548 ("Mermaid syntax NOT in FTS5") passes accidentally, AC at TASK.md:549 ("`SECTION:agent-metadata` NOT in FTS5") passes accidentally — neither verifies non-target content survives. | In R-07.5 spec the exact patterns: Mermaid: `r"^```mermaid\n.*?^```$"` with `re.DOTALL | re.MULTILINE` + a sanity check (`if not pattern.search(body): body_unchanged_count += 1`); anchors: `r"<!--\s*SECTION:[a-z0-9_-]+\s*-->"` (whitelist `SECTION:` prefix only — do NOT strip generic `<!-- ... -->`). Add AC: "Body with unclosed ` ```mermaid ` fence is rejected by R-07.5 (logged + warning), not silently truncated. Generic HTML comments (`<!-- TODO -->`) survive into FTS5." |
| **MED** | Karpathy-Drift | **UC-07 touches 1 file + 1 DB row + 1 log row. Karpathy's "Ingest" explicitly says "A single source might touch 10-15 wiki pages."** No cross-page update (no concept-page touch, no index regen trigger, no backlink injection). TASK acknowledges entity-promotion is Epic 7, but doesn't say "MVP intentionally violates Karpathy's 10-15 rule in exchange for X" — the gap is silent. | Spec reads like a port of Karpathy but the central promise (compounding cross-references) is deferred without acknowledgment. Reader expects wiki behavior, gets a glorified DB-row insert. | Add §6.1 row: "**Karpathy-deviation (MVP)**: ingest touches only the source page + index.md regen (R-08) + log row. Cross-page concept updates ⇒ Epic 7 (entity-resolver). Justification: type-safe entity resolution requires R-18, which is out of MVP scope." This is honesty, not new work. |
| **MED** | Logic | **`concepts[]` → tags merge has no slug normalization spec.** R-07.4 says "merge в tags JSON-array (slugified)". What does `slugify("C++")`, `slugify("API/REST")`, `slugify("OAuth 2.0")`, `slugify("RAG (Retrieval-Augmented Generation)")` produce? Library choice unspecified. Collisions (`"AI"` and `"a.i."` → `ai`) not addressed. | FTS5 keyword search misses concepts; collisions silently merge two distinct concepts into one tag. | Pin a library (`python-slugify` with `lowercase=True, separator='-', regex_pattern=r'[^a-z0-9-]'`) in R-07.4. Add AC: `slugify("C++") == "c"` (acknowledged loss); `slugify("OAuth 2.0") == "oauth-2-0"`. Note collisions in `pages.frontmatter_json.concepts_original[]` (preserve verbatim) vs `tags[]` (slugified). |
| **MED** | Spec-Gap | **§6.2 token estimate "$0.20-$0.60 per transcript" is unverified.** No model name, no input-token assumption, no current Anthropic price. The estimate says "Claude Sonnet" but the workflow file does not pin a model and the skill SKILL.md does not either. | Estimate cannot be audited or used as budget. R-14 benchmark suite has no LLM-cost dimension. | Replace with: "Assuming Claude Sonnet 4.5 @ $3/$15 per M tokens (input/output) and a 30-min transcript ≈ 40K input tokens + 15K output: ~$0.35 per transcript. For 150K-char (~50K input)+25K output transcript: ~$0.53. Confirm at runtime via `claude --version` + `wiki.transcript.model` config field. CI must record actual `llm_tokens_used` per benchmark run." Plus: tie this number to the `llm_tokens_used` field already in UC-07 step 12 return JSON. |
| **MED** | Logic | **UC-07 A3 silently swallows non-`lesson-summary` frontmatter.** "Treats as base summary (no R-07.4 mapping applied), proceeds with standard upsert." But what if the workflow returns `type: meeting-summary` (the workflow's *base* skill default is `meeting-summary` per SKILL.md L94 of the base skill)? §6.1 table says `meeting-summary → summary + tag` (future epic). A3 will write `pages.type='meeting-summary'` and fail the CHECK constraint. | INSERT raises `IntegrityError`. UC-07 has no handler. | Rewrite A3: "If `frontmatter.type ∉ §6.1 mapping`, fail-fast with `{error: 'UNMAPPED_TYPE', received: <type>, allowed: [...]}`. Do NOT attempt upsert with unknown type — CHECK constraint will reject and partial state may leak." Add the `meeting-summary` row to §6.1 mapping table NOW (even if marked "Epic 6 producer") so the CHECK-violation path is impossible. |
| **LOW** | Spec-Gap | **`<!-- SECTION:agent-metadata -->` marker check is necessary but insufficient.** I-3.3 (c) uses marker + `Content Fingerprint` block as "doneness" proof. But the workflow could write the marker, write `## Agent Metadata\n`, then crash before writing the fingerprint table → adapter sees both markers (one is just the marker, fingerprint is the literal heading string) and proceeds with a stub-laden summary. | False-positive "complete". | Strengthen the check: parse the `Content Fingerprint` block as a key/value list and require `Total concepts extracted: N` where N is parseable integer ≥ 1; OR require trailing-line equality `Source files: ...` (workflow's last template line). Add AC: "Adapter rejects summary where fingerprint block exists but `Total concepts extracted` is `{{N}}` (unrendered template)." |
| **LOW** | Spec-Gap | **Concurrent `/ingest-source --kind transcript` on the same source isn't addressed.** UC-02 A5 covers concurrent manual ingest of the same file via `BEGIN IMMEDIATE`. UC-07 has no equivalent. Two parallel transcript ingests both spawn the workflow → 2× LLM cost + race on output file. | Wasted LLM budget; file corruption if both write to `<output>/summary.md` concurrently. | Add UC-07 A8: "Concurrent ingest of same transcript: use file-lock on `<output>/.summary.lock` (flock) around steps (c)-(i). Second invocation waits, then sees A4 hash-match and short-circuits." |
| **LOW** | Hallucination Check | **TASK.md:11 cites `docs/KNOWN_ISSUES.md` as "будет создан при первом обнаружении"** — file does not exist. CLAUDE.md (project instructions) mandates "Read `docs/KNOWN_ISSUES.md` (Crucial to avoid repeating bugs)" in the Analysis phase. Pipeline currently violates its own instruction. | Pipeline step fails silently. | Either create an empty stub `docs/KNOWN_ISSUES.md` with header now, or remove the citation from TASK.md:11 and update CLAUDE.md to make the file optional. |

---

## 3. Hallucination Check

Verified each cited path/section:

| Cited in TASK | Exists? | Notes |
|:---|:---|:---|
| `docs/SCHEMA-DRAFT.sql` | YES | 492 lines; verified `pages.type` CHECK enum matches §6.1 mapping table |
| `docs/SQLITE-VS-POSTGRES.md` | YES | |
| `docs/MIGRATION-v1-to-v2.md` | YES | |
| `docs/TASK-ref-v2.md` | YES | 90KB |
| `docs/KNOWN_ISSUES.md` | **NO** | TASK acknowledges it's missing (line 12), but CLAUDE.md mandates reading it → see LOW issue above |
| `~/.claude/skills/summarizing-meetings/SKILL.md` | YES | confirmed L81 "> 100K chars → chunk into ~50K blocks with 2K overlap" — supports §6.2 multi-pass claim |
| `~/.claude/commands/generate-detailed-meeting-summary.md` | YES (symlink) | **Points to `/Users/sergey/dev-projects/Universal-skills/commands/generate-detailed-meeting-summary.md`, a 1-line redirect to `workflows/generate-detailed-meeting-summary.md`.** I-3.3 step (a) only checks file presence; it does NOT validate that the file is the *actual* workflow vs the redirect stub. Path holds, but the adapter must call `claude -p` to dereference it — the path check alone is meaningless. |
| `Universal-skills/workflows/generate-detailed-meeting-summary.md` | YES | The real workflow, 358 lines. TASK.md:486 cites this path as an alternative; correct. |
| TASK §5.1 latency table refs from UC-03/UC-04 AC | YES | Numbers in table match UC ACs (30/50/100 ms, 500/2000/30000 ms) |
| §6.1 type-mapping table | YES (new) | All 4 producers map to `pages.type` values present in SCHEMA L109 CHECK enum |
| `vault_metadata` (R-25) | YES | Confirmed in SCHEMA L273-277 |
| `source_state` table (referenced indirectly in my recommendations) | YES | SCHEMA §8 L293-300 — usable for transcript_source_hash |
| `SECTION:agent-metadata` marker | YES | Verified at workflow L157 and L292 |
| `Content Fingerprint` block | YES | Workflow L327-333 |
| TASK.md citing `tmp2/` 16 files | UNVERIFIED | Did not enumerate; not load-bearing for this review |
| Karpathy `llm-wiki.md` "10-15 wiki pages" claim | YES | Verified L37 of `Reference/karpathy/llm-wiki.md` |
| ARCHITECTURE.md transcript adapter description | YES | L71 says "wraps `summarizing-meetings` skill через subprocess" — **drift with TASK**: TASK now says it wraps the WORKFLOW, not the base skill. ARCHITECTURE.md L421-425 also reads as base-skill wrapping. Recommendation: update ARCHITECTURE.md to match TASK's R-06.3 wording, or note the inconsistency in the Architecture phase. |

---

## 4. Convergence Signal

Not converging on hallucinated problems. Every HIGH issue is grounded in a specific column / regex / file path I verified, and every recommendation is implementable as a concrete diff. The MED issues are equally grounded but lower blast radius. I stopped short of nitpicking phrasing because the task said "density > verbosity."

The one place I felt the urge to fabricate was around the FTS5 trigger interaction with R-07.5 stripped body — but the SCHEMA's `pages_fts_insert` trigger reads `new.body_excerpt` directly, so the stripping happens at the Python layer before INSERT, and the trigger does the rest correctly. I confirmed by reading SCHEMA L329-339. Not a real issue.

If the four HIGH items are fixed (idempotency landing place, subprocess contract with timeout/cleanup, lint-aware mapping, explicit R-07.5 regex), this TASK is genuinely ready for Architecture phase. **Zero-Slop achieved.**
