pyyaml>=6.0
python-frontmatter>=1.0
python-slugify>=8.0
jsonschema>=4.20
regex>=2024.0
# TASK 028: pinned EXACT (not >=) ON PURPOSE — a stem-algorithm change alters the
# retrieved hit set, which feeds wiki-query's question_hash → would break filed-answer
# reproducibility. A bump is a deliberate re-query/reindex-affecting event. Pure-Python
# (py3-none-any wheel, no C extension).
snowballstemmer==3.1.1
pytest>=7.4
pytest-cov>=4.1
mypy>=1.7
types-PyYAML>=6.0
types-jsonschema>=4.20
types-regex
ruamel.yaml>=0.18
