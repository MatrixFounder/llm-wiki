# scripts/wiki_index/ — DAL Module Memory

**Purpose**: Data Access Layer for the multi-vault SQLite index. All read/write
to the DB goes through `IndexRepository` (abstract) and `SQLiteRepository`
(concrete). Skills never construct raw SQL — they call repository methods.

## Files (planned for Phase 3a)

- `repository.py` — `IndexRepository` ABC + dataclasses (Page, Entity, Vault,
  PageHit, LogEvent, OrphanLink, BatchRun). Defines the DAL contract. (task-001-03)
- `sqlite_repository.py` — concrete impl using `sqlite3` stdlib. WAL pragmas,
  parameterized queries, `BEGIN IMMEDIATE` transactions. (task-001-04 stub →
  task-001-15..19 impl).
- `factory.py` — `make_repo(config)` selects backend; rejects iCloud paths;
  returns ready connection. (task-001-05 stub → task-001-20 impl)
- `config_loader.py` — `load_config(cwd)` walk-up + deep-merge + JSON Schema
  validation. (task-001-13)
- `icloud.py` — platform DB path resolver + iCloud detection (R-03). (task-001-14)
- `reindex.py` — `wiki-reindex --full` impl: Class A → B reconstruction (ADR-002
  §D8 rebuildability invariant). (task-001-30)
- `security.py` — `validate_inside_vault(path, vault_root)` path-traversal
  guard (R-26). (task-001-12)

## Class A/B/C discipline (ADR-002 §D8)

This module owns the DB-side cache. Everything it writes is Class B (rebuildable
from markdown) except `vaults.registered_at` (Class C strict — approximated on
reindex). When adding a new column or table:

1. Classify it (A/B/C). If unsure, default to B and document the rebuild path.
2. If Class A — DO NOT add to schema. Belongs in vault markdown only.
3. If Class C — minimize. Justify why view/computed value won't work.

## Upsert contract (M-4)

`pages` and `entities` upserts MUST use
`INSERT … ON CONFLICT(<composite>) DO UPDATE SET …`. **NEVER**
`INSERT OR REPLACE` — see `sql/.AGENTS.md`. The repository must enforce this
at the SQL-string level (grep-test in CI: no `INSERT OR REPLACE` strings in
this directory).

## Read more

- `sql/wiki-index-v2.sql` — DDL
- `docs/adr/ADR-001`, `docs/adr/ADR-002`
- `docs/PLAN.md` — Phase 3a task dependencies
