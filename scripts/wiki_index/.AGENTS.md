# scripts/wiki_index/ — DAL Module Memory

**Purpose**: Data Access Layer for the multi-vault SQLite index. All read/write
to the DB goes through `IndexRepository` (abstract) and `SQLiteRepository`
(concrete). Skills never construct raw SQL — they call repository methods.

## Files (Phase 3a complete)

- `repository.py` — `IndexRepository` ABC + dataclasses (Page, Entity, Vault,
  PageHit, LogEvent, OrphanLink, BatchRun, DriftReport, PageRef). DAL contract.
  **Post-TASK 003 v2 (I-7.7a, 2026-05-28)**: added `upsert_entity(...)` abstract
  method — write-path counterpart to the Phase-3a `resolve_entity` read-path
  stub. Required by `wiki_extract_concepts.py` to write candidate concept
  entities.
- `sqlite_repository.py` — concrete `SQLiteRepository`. WAL pragmas, parameterized
  queries, `BEGIN IMMEDIATE` transactions. Implements every abstract method
  plus `_in_clause` helper for vault-scoped IN queries. **Post-TASK 003 v2
  (I-7.7a)**: added `upsert_entity()` impl using `INSERT … ON CONFLICT
  (vault_id, slug) DO UPDATE SET is_candidate = MIN(excluded.is_candidate,
  entities.is_candidate)` — SQL-level downgrade guard (R-37b) so a confirmed
  entity (`is_candidate=0`) cannot be demoted to candidate by a re-extraction,
  regardless of caller correctness. `file_path` column populated (required by
  the schema's `UNIQUE(vault_id, file_path)` constraint).
- `factory.py` — `make_repo(config)` selects backend; rejects iCloud paths;
  per-platform default DB path; `validate_db_path`. Accepts `_global_` sentinel
  vault_id for cross-vault CLI operations.
- `config_loader.py` — `find_vault_root` walk-up, `load_root_config`,
  `deep_merge` (lists replaced not concatenated per R-01.2), JSON Schema
  validation against `#/$defs/WikiRootConfig`.
- `layout.py` — **Single source of truth for layout constants**: `PAGE_SUBDIRS`,
  `COURSE_TIER_DIR` (`Lessons`), `VAULT_INDEX_DIR` (`00-Vault-Index`),
  `LOG_SUBDIR`, `SCAFFOLD_DIRS`, `SYSTEM_FILES`, `GLOBAL_VAULT_SENTINEL`.
  Modules MUST import from here — do NOT re-literal directory names elsewhere.
- `lint.py` — `LintIssue` dataclass + `run_all_checks` orchestrator +
  `render_markdown_report` / `render_json_sidecar`.
- `logfile.py` — `rotate_log_path` (monthly), `render_event_line`,
  `append_atomic` (fcntl.flock + os.fsync + short-write loop), `parse_log_md`
  (reverse parse for reindex round-trip).
- `models.py` — frozen dataclasses + type aliases (`PageType`, `TrustLevel`,
  `BatchMode`, `BatchStatus`).
- `normalization.py` — `TYPE_MAPPING` (R-07.4), `normalize_frontmatter`,
  `normalize_body_for_fts` with mermaid + SECTION anchors stripping +
  anti-tail-eat sanity check.
- `reindex.py` — `discover_pages` (two-tier walk), `reindex_full` (Class A→B
  rebuild, ADR-002 §D8), `reindex_delta` (mtime-based, exception-skipped
  recorded; atomic orphan delete tx + begin/finish batch_run).
- `rendering.py` — `extract_custom_sections` (preserves
  `<!-- BEGIN-CUSTOM:name -->` blocks), `render_index`, `atomic_write`
  (tempfile + os.rename).
- `security.py` — `validate_inside_vault(path, vault_root)` (R-26),
  `assert_no_symlink_escape` (defensive sanity rail; see KNOWN_ISSUES D-1
  for documented limits).

## Class A/B/C discipline (ADR-002 §D8)

This module owns the DB-side cache. Everything it writes is Class B (rebuildable
from markdown) except `vaults.registered_at` (Class C strict — approximated on
reindex). When adding a new column or table:

1. Classify it (A/B/C). If unsure, default to B and document the rebuild path.
2. If Class A — DO NOT add to schema. Belongs in vault markdown only.
3. If Class C — minimize. Justify why view/computed value won't work.

## Upsert contract (M-4)

`pages` and `entities` upserts MUST use
`INSERT … ON CONFLICT(<composite>) DO UPDATE SET …`. **NEVER**
`INSERT OR REPLACE` — see `sql/.AGENTS.md`. The repository must enforce this
at the SQL-string level (grep-test in CI: no `INSERT OR REPLACE` strings in
this directory).

## Read more

- `sql/wiki-index-v2.sql` — DDL
- `docs/adr/ADR-001`, `docs/adr/ADR-002`
- `docs/PLAN.md` — Phase 3a task dependencies
