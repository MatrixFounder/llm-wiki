# scripts/wiki_skills/ — CLI Skill Scaffolds

User-facing entry points. Each module is invokable as
`python -m scripts.wiki_skills.<name> ...`. All modules now ship real impls
(Phase 3a complete).

## Files

- `wiki_init.py` — UC-01; modes: `--scaffold-new` / `--register-existing` /
  `--reconcile`. Threads its own `_emit` with per-error `exit_code`. **TASK 012 /
  R-X2-A** introduced the `--layout` flag; **TASK 031 / R-031-3** de-hardcoded its
  registry: the `--layout` choice-set and the two-tier-scaffold family are now
  SOURCED from the built-in `layouts/*.yaml` registry via
  `layout_config.layout_choices()` + `is_two_tier_scaffold(<layout>)` (each layout
  declares optional `aliases`/`init_scaffold`) — no hardcoded list/set lives here.
  Only layouts declaring `init_scaffold: two-tier` (`karpathy` + its `flat`/
  `per-project` aliases) scaffold the Karpathy dir tree; `dev-project`/
  `obsidian-personal`/`cybos` skip it. A new built-in layout is a drop-in YAML.
  Writes the vendor agent file(s) + `.claude/settings.json` (`_write_agent_files`,
  TASK 025/026) AND — for the SAME non-two-tier layouts — **copies the 13
  `templates/page-types/*.md` scaffolds into `<vault>/.wiki/page-types/`**
  (`_write_page_type_templates`, VERBATIM, per-file non-destructive; envelope key
  `page_type_templates`) so a vault-launched agent has the typed-class authoring
  templates locally. Target is under `.wiki/` (dot-pruned by the walk → never indexed
  as junk; the safety pin is `test_copied_page_types_are_never_indexed`). Karpathy
  family → no copy.
- `wiki_search.py` — UC-03 FTS5 search. Cross-vault via `--vaults` (or
  `_global_` sentinel for "all"). **TASK 013 (R-X3-META-FILTER)**: frontmatter
  **metadata filter** — `--where 'field=value'` (repeatable, AND) + `--status`/
  `--severity`/**`--tag` (TASK 033)** sugar (desugar into `where_fields`; `--tag v`
  ⇒ `("tags", v)`). `_parse_where` splits on the first `=`, strips, and
  allow-list-validates the field via `repository.validate_filter_field` (the value is
  never validated — it becomes a bound param). **TASK 033 (R-1):** the DAL predicate
  now matches a list MEMBER too (scalar `=` OR `EXISTS(json_each … = ?)` — see
  `wiki_index/.AGENTS.md`), so `--where 'tags=decision'` / `--tag decision` lists every
  typed-class page in one command (back-compatible: scalar filters unchanged). The
  positional `query` is now `nargs='?'`: omit it for a **pure
  metadata listing** (non-FTS path — skips alias-expansion + the DF-1 FTS-quote
  fallback). **TASK 035 (ADR-005):** a query-less `--tag`/`tags=` listing transparently
  narrows through the `pages_fts.tags` index in the DAL (~4× faster at scale) — CLI surface,
  results, and exit codes are UNCHANGED (see `wiki_index/.AGENTS.md`). Bad field / malformed
  `--where` / **two predicates on the same field**
  (e.g. `--status open --where 'status=fixed'` — equality-only, so it could never
  match) → `INVALID_FILTER` (exit 2) that **never echoes the value** (CWE-209/117);
  neither query nor filter → `INVALID_QUERY`.
  **TASK 034 (R-1): temporal `--as-of YYYY-MM-DD`.** Validated+normalized at the CLI
  boundary via `date.fromisoformat(strip()).isoformat()` (only the canonical ISO output
  reaches the DAL — the raw arg is discarded; a non-ISO date → `INVALID_FILTER`, value
  never echoed) and threaded to `search_pages(as_of=)` (both FTS + non-FTS call sites).
  Counts as a searchable filter (relaxes the empty-search guard: `query` ∧ `where_fields`
  ∧ `as_of` all empty → `INVALID_QUERY`). Returns pages **active on that date** —
  derived from `pages.date` + the supersede/invalidate graph (see `wiki_index/.AGENTS.md`);
  `valid_from`/`valid_to` are optional overrides, not required. Composes (AND) with the
  query + the metadata filters.
  **TASK 028 (R-028-2): default-on stemming + ё/е fold.** Builds the MATCH via
  `_retrieval.build_search_query` (stem-first → OR-in exact alias surfaces, F-1) instead of
  `expand_query`; `--exact`/`--no-stem` (`store_true`, default off ⇒ stemming ON) disables
  stemming only (ё/е fold stays). The DF-1 `OperationalError` fallback is now ё-fold-aware
  (`fold_yo(fts_quote(raw))`). **vdd-multi fix:** the query is `.strip()`-ed at the boundary
  (`query_arg`) — a whitespace-only query (which the lexer collapses to `''`, and
  `search_pages('')` rejects with a `ValueError` the DF-1 net never caught) is now a clean
  `INVALID_QUERY` (exit 2), and the echo reports the stripped query.
- `wiki_lint.py` — UC-04 SQL-level lint. Optional `--report` md /
  `--json-sidecar` outputs.
- `wiki_index_render.py` — UC-05 `index.md` projection. Preserves
  `<!-- BEGIN-CUSTOM:name -->` blocks across renders. **TASK 012 / PW-H**:
  `--auto-indexes` flag re-renders the layout's `auto_indexes[]` outputs (e.g.
  `docs/KNOWN_ISSUES.md` from `docs/issues/*.md`) via
  `rendering.render_and_write_auto_indexes` — the Class-B rebuildable ledger.
- `wiki_append_log.py` — bi-directional log.md ↔ log_events sync. Emits
  `LOG_APPEND_FAILED` envelope on write failure (rolls back log_events row).
- `wiki_index_upsert.py` — single-file ingest. **TASK 015 / R-015-1**:
  `upsert_one(vault_id, src, vault_root, repo) → dict[str,Any]` is the
  programmatic entry-point (no argparse, caller-owned repo, returns envelope
  dict with private `_exit_code` key). `main()` delegates to `upsert_one`,
  pops `_exit_code`, and calls `emit()`. Stub-compatible `unchanged` return
  when file_hash unchanged. **TASK 024 / Q-024-1**: `upsert_one` is now
  **layout-aware** — it `resolve_layout_config(vault_root)` + `derive_discovered_page`
  + routes through the shared `reindex.derive_indexed_page`, so a single-file upsert
  files **byte-identically to `reindex`** (project via project_pattern, slug via
  slug_strategy, type via the layout's type_mapping, refs via ref_extraction + slug
  slugify). Was `derive_slug`'s `_vault_` fallback + the karpathy module `TYPE_MAPPING`
  → diverged on PARA/`obsidian-personal` (`_vault_` rows + dup-on-`reindex --full`).
  Error contract unchanged (`UnmappedTypeError`/`BodyNormalizationError` → exit 6).
- `wiki_reindex.py` — `--full` / `--delta` / `--all-vaults` modes.
- `wiki_confirm.py` — **TASK 005 / R-4.2/4.3/4.4** (Epic 7 entity resolver).
  Promote candidate→confirmed (`--undo` demotes; `--auto --threshold N`
  bulk-promotes by mentions, default 3; `--dry-run`). Class A frontmatter
  (`is_candidate`) written FIRST, then `repo.set_entity_candidate` (bypasses the
  re-extraction MIN() guard). Exit 0/2/3/4. The optional `promote`/`demote`
  log_events row (Q5) is deferred (needs log.md M-2 mirroring).
- `wiki_alias.py` — **TASK 005 / R-5.1/5.2**. `--add`/`--remove`/`--list` alias
  surfaces; Class A `aliases:` frontmatter + `entity_aliases` mirror. Collision
  → `ALIAS_COLLISION` (exit 5, names the conflicting slug, never the surface).
  **TASK 014 / R-MF14-3**: the `slug` positional is now optional — `--list` with NO
  slug lists EVERY alias in the vault (via `repo.list_all_aliases`); `--add`/
  `--remove` with no slug → `SLUG_REQUIRED` (exit 2).
- `wiki_merge.py` — **TASK 005 / R-4.7**. Fold duplicate `from`→`into`: Class A
  first (append `into.aliases` + delete `from` page) then `repo.merge_entities`.
  Alias table is the durable redirect (C-7, no wikilink rewriting). Exit
  0/2/3/4/5(`INVALID_MERGE`)/6(`MERGE_MIRROR_FAILED`).
- `wiki_search.py` / `wiki_lint.py` — **TASK 005 extensions**: search expands
  queries through aliases by default (`--no-expand-aliases` opt-out, R-5.5);
  lint adds an `alias-collision` category (DB + Class A frontmatter scan, R-5.6;
  `--strict` → non-zero advisory exit). **TASK 017 (P-3)**: `wiki-lint --mtime-skip`
  (default off) threads `mtime_skip` → `run_all_checks` → `check_drift(trust_mtime=)` —
  skips the drift re-hash for mtime-unchanged files (integrity-relaxed; default full-hash).
- `_common.py` — shared CLI helpers: `emit` (JSON envelope), `atomic_write_text`
  (tempfile+fsync+`os.replace`), `resolve_entity_file` (entity `file_path` →
  validated absolute path via `get_entity_file_path` + `validate_inside_vault`),
  and `sanitize_markdown_text` (text-only allowlist egress guard — **TASK 007**
  lifted it here from `wiki_extract_concepts` per Decision-16 so that skill and
  `wiki_query` share one implementation). **TASK 022 (vault-local DB):**
  `build_repo_config(vault_id, *, vault_root, db_path_flag)` — the chain
  `--db-path > index_db > global` → a `make_repo` config dict (lazy-imports
  `config_loader`; `make_repo` unchanged) — and `resolve_vault_root_for_cli(args)`
  (`--vault-root` flag → `find_vault_root(cwd)` walk-up → None). Every CLI resolves
  `vault_root` and builds config via these **before** `make_repo` (the ordering
  inversion), so a vault-local `index_db` is honoured uniformly across all
  subcommands (no `record`/`apply`/enrich split-brain). Island model: `--vault all`
  spans only the connected DB (no cross-DB federation). **`/vdd-multi`-hardened:**
  `build_repo_config` passes `expected_vault_id` (sentinel→None) so a walk-up can't open
  a different vault's DB, and on a malformed/unsafe `index_db` it `emit`s a single
  `INVALID_INDEX_DB` envelope + `SystemExit(6)` (no raw traceback, no value echo). See
  `config_loader.resolve_index_db_path` for the leaf-symlink + absolute-gate
  (`WIKI_ALLOW_ABSOLUTE_INDEX_DB`, with the TASK 042 app-data carve-out) containment.
  `wiki_init._validate_index_db_rel` bans
  every YAML line-break (incl. U+0085/U+2028/U+2029) + `": "` before writing `index_db:`.
- `wiki_query.py` — **TASK 007 / R-6** RAG over FTS5 + entity graph. Two-pass
  Decision-17 skill (no `import anthropic`): `prepare` (alias-expanded retrieval
  → context envelope; `NO_CONTEXT` below `--min-hits`; default-excludes
  `type=query` so a RAG answer grounds on primary sources + re-query stays
  idempotent) and `apply` (re-retrieve + `question_hash` TOCTOU → `QUESTION_CHANGED`;
  grounding gate `CITATION_NOT_RETRIEVED` keyed on `project/slug`; sanitise +
  atomic-write Class A `_queries/<slug>.md`; self-index via **direct**
  `upsert_page`+`replace_refs` (NOT the manifest N+1) + `record_query_state` +
  a `query` log event). `apply` mirrors `prepare`'s retrieval-scope flags to
  reproduce the hash. **TASK 014 / R-MF14-2**: `--vault-root` is now optional on
  both subcommands — `_derive_vault_root` falls back to the registered vault's
  `root_path` when omitted (explicit flag wins; `prepare` never read it anyway).
  **TASK 028 (R-028-3): per-token stemming + ё/е fold.** `_build_match_query` stems each token
  BEFORE `fts_quote` → `"<stem>"*` (wiki-query already quotes every token, so an FTS-expr
  stemmer would no-op here — F-2); alias surfaces use the RAW token, folded+quoted, never
  stemmed; the final `sorted` join anchors `question_hash` determinism (C1). `--exact`/`--no-stem`
  is on BOTH `prepare` and `apply` (threaded through the shared `_retrieve`) so the hash matches —
  asymmetric flags → `QUESTION_CHANGED` (documented). DF-1 fallback ё-fold-aware. A whitespace/
  punct-only question is already safe (`if not tokens: return fts_quote(fold_yo(question))`).
- `wiki_verify_multi.py` — **TASK 008 / R-8** off-by-default multi-critic
  verification of a filed `wiki-query` answer. Two-pass Decision-17 skill (no
  `import anthropic`): `prepare <query-slug>` (load the query page + cited source
  bodies via **`pages.file_path`** — layout-agnostic, grep-guarded, no
  PAGE_SUBDIRS literal; `NO_SOURCES` on empty `cites:`; emits the verification
  envelope; `verification_slug` defaults to **`verify-<query-slug>`** — distinct
  `pages` PK so the verdict page does NOT overwrite the query page) and `apply`
  (re-read query page + `answer_hash` TOCTOU → `ANSWER_CHANGED`; grounding gate
  `FINDING_SOURCE_NOT_EXAMINED` keyed on `project/slug`; **Python-derived**
  PASS/FAIL via `--fail-on` (default `high`, overrides the LLM's self-report);
  sanitise + atomic-write Class A `_verifications/verify-<slug>.md`; self-index
  via **direct** `upsert_page`+`replace_refs(verifies[+cited])` (NOT the manifest
  N+1) + `record_verify_state` + a `verify` log event). **FAIL → exit 6** (a
  SUCCESS envelope, no `error` key — deliberate divergence from the family's
  `6=error`; callers branch on the stdout `verdict`). **The Class-A answer is
  NEVER mutated** (D-008-3).
- `wiki_graph.py` — **TASK 032 / R-032-5 (ADR-004)** read-only **event-graph** traversal
  CLI (the 16th; `bin/wiki-graph` wrapper). Subcommands `backlinks`/`neighbors`/`chain`
  over the typed page-to-page edges (DAL: `get_backlinks(ref_type=)` / `refs_from` /
  `neighbors` / `edge_chain` — BFS, deque, cycle-safe, depth-capped). Injection-safe:
  `--kind` allow-listed (`_EDGE_KINDS`, no value echo on `INVALID_KIND`), slug/project
  bound params; `MISSING_KIND`/`INVALID_DEPTH` exit 2, `VAULT_NOT_FOUND` exit 6;
  `--depth` capped (`_MAX_DEPTH=16`). **TASK 034 (DF-034-1 fix):** `_EDGE_KINDS` is now
  **DERIVED** `tuple(sorted(reindex._INVERSE_REF_TYPE))` — a single source of truth, so
  the v7 edge kinds (`invalidated-by`/`activated-by`/`uses`/`owns` + inverses) traverse
  automatically and the allow-list can never drift from the extractor (the dogfood caught
  the old hardcoded list silently dropping them as `INVALID_KIND`). Pairs with
  `wiki_query.py --follow-edges` (graph-aware
  RAG, default OFF, deterministic `question_hash`; `via_edge` provenance on edge-pulled hits).
- `wiki_sync.py` — **TASK 018 / R-11** format-aware, tag-routed ingest
  dispatcher; **TASK 046 — converged into a pure batch DRIVER** that delegates each
  distil source to **`wiki-import`** (which owns convert + de-timestamp + REASON + file +
  index + concepts); no inline summarise/enrich/extract/convert. Two deterministic
  subcommands (no `import anthropic` — grep-guarded):
  **`scan <zone>`** walks a zone (`_sync.iter_sync_candidates`, its OWN bounded
  walk — not `iter_pages`), classifies each file (`_sync.classify_file`),
  `sha256`-hashes the bytes, reads `is_unchanged` via the `sync` `source_state`
  partition, and emits a strict **plan JSON** (`entries[]` sorted by vault-rel
  POSIX path → deterministic, no timestamp; `--dry-run` = human report, writes
  nothing). Each `ingest`/`convert+ingest` entry carries a **`delegate`**
  (`{tool:"wiki-import", source, folder, kind, diagrams, concepts}`, from the per-folder
  `summarize:` config) — the orchestrator runs the `wiki-import` loop per delegate; ready
  `.md` → `wiki-index-upsert`. **`record <rel> --source-hash`** = the executor's
  post-success commit-marker (`set_source_state(vault,'sync',rel,'source_hash',…)`; a
  delegated import records BOTH the original source AND `wiki-import`'s `_raw` capture →
  no re-ingest loop; validates
  64-hex + clean-rel; FK-miss → `VAULT_NOT_REGISTERED`). Exit `0`/`2`/`6`;
  envelopes never echo file content. The orchestrator recipe is
  `workflows/wiki-sync.md`; the contract is `skills/wiki-sync/SKILL.md`. **TASK 046 H-6**:
  distil is delegated, so the nonce-sentinel fence lives in the **wiki-import reason-contract**
  (`references/reason-contract.md`, Hard Rule #4) and rides the delegation — there is **no
  separate wiki-sync fence step**.
  **TASK 019**: `_build_entries` now threads a `force` flag (`scan --force`) and
  runs the `_resummarize.apply_policy` gate between `classify_file` and the plan
  entry; per-folder configs read by the cascade resolver also map to exit 6.
  **TASK 046 P3**: `_build_entries` also resolves the per-folder `summarize:` cascade
  (`_resummarize.resolve_summarize`, sharing the `Caches.validated` per-dir raw read with
  the `resummarize` cascade) to populate each entry's `delegate` knobs.
- `_resummarize.py` — **TASK 019** the re-summarization policy: a **monotone** gate
  (only `ingest`/`convert+ingest` → `skip`, never touches `upsert`/`skip`). A per-scan
  **`Caches`** (built once in `_build_entries`) hoists the expensive work out of the
  per-file loop (vdd-multi PERF): `resolve_policy(path,*,vault_root,caches)` = the
  per-folder **Option-A cascade** (reads each ancestor `<dir>/.wiki/sync.yaml`
  `resummarize:` via `sync_config.load_resummarize_raw`, deep-merges deepest-wins on
  RAW dicts so a partial folder override inherits the parent `detect`; **parsed policy
  memoized per parent dir**). `summary_exists(...)` = cheapest-first
  D1 (`source_state` presence) ∪ D2a (membership in `repo.all_cited_sources` —
  hoisted **once per `(fields, match)`**; `match` ∈ vault-rel-path | basename;
  **TASK 024 / R-4**: both the cited set + the target are `unicodedata.normalize("NFC", …)`
  so a macOS-NFD FS path matches an NFC frontmatter `sources:` — a Cyrillic-named raw
  otherwise re-converts every scan; D1/D2b are FS-self-consistent so the fix is D2a-only) ∪ D2b
  (`_mirror_match` — `stem-relpath` 1:1 / `group-key` N:1 via a configurable
  `key{raw_regex,summary_regex,template,flags}`; the summary-key index is built **once
  per scope** under a SINGLE shared ReDoS deadline; the `scope` is `validate_inside_vault`-
  contained + `rglob(recurse_symlinks=False)`). Operator regexes pass a cached
  **load-gate** (`layout_config.is_pattern_redos_safe`, → `SyncConfigError`/exit 6, no
  echo) AND a per-call `guarded_search` deadline (timeout = no-match, never hangs).
  `apply_policy(...)` = mode (if-missing/always/never) + `--force` (uniform reason
  `forced`). **Dead-detector WARN** (post-ship dogfood): `_scope_key_index` builds the
  summary-key set BEFORE the raw-side `rkey` short-circuit and logs once per scope when
  mirror is enabled but the `group_key`/`key` regex keys 0 of N summaries (catches a
  silently-inert detector, e.g. a YAML double-backslash `'^(\\d+)'`). **Merge/split WARN**
  (TASK 021 / HIGH-1, Option A): `_scope_key_index` returns a `key→representative-summary`
  map; on an N:1 `group-key` skip where provenance is enabled but does NOT cite the raw
  (`summary_exists` passes `warn_uncited=pr.enabled` to `_mirror_match`), it logs the
  merge-vs-split decision (`--force` to merge / finer key to split). **Skip is unchanged**
  (behaviour-preserving visibility); `stem-relpath` (1:1) never warns. Zero DDL; no
  `import anthropic`. Acyclic (imports `Decision` from `_sync`; `_sync` does NOT import this).
- `_sync.py` — **TASK 018** the routing brain (pure, no I/O mutation).
  `classify_file(path, *, vault_root, config, layout, in_raw, in_exclude_zone) ->
  Decision` — extension front-stage (case-folded; `.excalidraw.md`/`.canvas`/
  `.base` before generic `.md`) → tag precedence **skip>raw>keep>default**
  (`#<ns>/x` tags NOT inside fenced blocks + a `<ns>:` field; `_raw/`≡raw; `keep`
  rescues an `exclude:` zone) → generated-view sidecar + **only-a-view guard**
  (DB Folder / Bases / Dataview / folder-note → skip ONLY when the note is
  essentially one view block; embeds-a-view-alongside-prose → upsert) →
  unmappable-type via the **same `normalize_frontmatter` layout resolution** the
  indexer uses (W-1; ANY predictor fault incl. malformed `type:`/`tags:` →
  `skip:unmappable-type`, never raises — AC-11). `iter_sync_candidates` = the own
  walk (one `lstat`/surviving candidate; ext-prune-before-stat; prune
  `_raw/.staging`+`_raw/.locks`+`_raw/failed`+dot-dirs; refuse symlinked dirs +
  target files; read `.md` in `exclude:` zones for keep-rescue). RC-5 decision
  (do NOT reuse `wiki_ingest._classify._count_md_structure` — its `lru_cache`
  staleness) recorded inline. **vdd-multi hardening**: a `.md` over
  `WIKI_SYNC_MD_MAX_BYTES` (8 MiB) → `skip:oversize-source` BEFORE `read_text`
  (the only unbounded-RAM lever, bounded); `_parse_frontmatter` strips a leading
  UTF-8 BOM (parity with the indexer's `wiki_ingest._frontmatter`, else a valid
  Windows note is mis-skipped); `_ext_route_sets` is `lru_cache`d on the frozen
  config.
- `_retrieval.py` — **TASK 007 / C-6** shared FTS retrieval (`expand_query` +
  `fts_quote`) extracted from `wiki_search`; imported by both `wiki_search` and
  `wiki_query` so the alias-expanded retrieval lives in one place (wiki-search
  output byte-identical). **TASK 028 (F-1):** split `alias_surfaces` (the gather half) out of
  `expand_query`, and added `build_search_query(repo, raw, vaults, *, stem, expand)` — the
  wiki-search MATCH builder that stems+folds the bare query FIRST (`query_normalizer.stem_fts_query`)
  then OR-in the (folded, quoted, **unstemmed**) alias surfaces `(<stemmed>) OR "alias"` — NOT
  `stem(expand_query(raw))`, which would quote the whole raw query and broaden nothing on an alias
  hit. Empty-base guard (`if not base.strip(): return base`) so a blank query never paren-wraps to
  `()`. `expand_query` retained for back-compat.
- `wiki_enrich.py` — bridge to `wiki-ingest` (ADR-001 Option I). Post-TASK
  004 (2026-05-27): **two-path design**. PRIMARY = in-process call into
  vendored `scripts.wiki_ingest.commands.ingest.ingest()` — no subprocess,
  no PATH dependency. FALLBACK = legacy subprocess via `wiki-ingest` CLI
  on PATH. Decision: `_VENDORED_AVAILABLE and not _force_subprocess`,
  where `_force_subprocess` reads `WIKI_ENRICH_NO_VENDORED` env-var
  (case-insensitive truthy set `{1, true, yes, on}`, whitespace-stripped
  — per `/vdd-multi` 2026-05-27 H-3 hardening). If vendored import fails
  AND `wiki-ingest` not on PATH → exit 6 with `{"error":
  "WIKI_INGEST_UNAVAILABLE"}`. `IngestError` from the vendored path is
  content-level — does NOT fall back to subprocess. **Post-TASK 003 v2
  (Decision-16)**: `validate_manifest`, `index_from_manifest`, and
  `WikiIngestError` no longer live in this file — moved to
  `_manifest_consumer.py`. This module re-exports the three symbols
  (back-compat alias `_validate_manifest = validate_manifest` kept one
  release cycle for existing test imports). `index_from_manifest` (now
  in `_manifest_consumer.py`) applies (a) R-0 top-level-system-file
  skip + (b) `/vdd-multi` absolute-path-rejection guard + (c) narrow
  exception catch `(OSError, ValueError, KeyError, RuntimeError)`.
- `_manifest_consumer.py` — **neutral sub-layer module** (TASK 003 v2 /
  Decision-16 / I-7.0). Holds `validate_manifest`, `index_from_manifest`,
  `WikiIngestError`. Neither this module nor its consumers belong to a
  specific skill; placing them here breaks the "no skill imports from
  another skill" coupling that Decision-15's first draft would have
  introduced. **TASK 015 / R-015-2**: `index_from_manifest` now imports
  `upsert_one` at module load (no lazy import) and accepts an optional
  `repo: Any = None` parameter — when provided, the caller's connection is
  reused (no open/close cycle); when None, one connection is opened and
  closed internally. Eliminates N argparse+connection cycles per invocation
  (H-PERF-3 + P-8). Both `wiki_enrich.py` and `wiki_extract_concepts.py`
  import from this module.
- `wiki_extract_concepts/` — **TASK 003 v3.1 entry-point** (Epic 7 R-3,
  shipped 2026-05-28 commit `43812f2`; supersedes v2). Decision-17:
  deterministic two-subcommand surface (`prepare` + `apply`); the LLM
  call lives in the **calling agent's** context, not this module — see
  `skills/concept-extraction/SKILL.md` for the synthesis contract.
  - **TASK 016 (package split)**: was a 2174-line single module; now a
    **package** `wiki_extract_concepts/` — facade `__init__.py` (orchestration:
    `prepare`/`apply`/`dispatch_to_indexer`/`_batch_*`/`_apply_*`/`main`/
    `_build_parser_v3`) + leaves `_errors` (sink), `_validation`, `_sourcing`,
    `_db`, `_pages`; `__main__.py` for `python -m`. **Patch-target-lock invariant
    (R-016-2)**: the 8 monkeypatched names (`make_repo`, `load_known_entities`,
    `validate_manifest`, `index_from_manifest`, `dispatch_to_indexer`,
    `_apply_candidates_to_db`, `_try_update_idempotency_state`,
    `update_idempotency_state`) are bound as **facade globals** — callers in
    `__init__.py` resolve them there, so `mock.patch("…wiki_extract_concepts.<n>")`
    intercepts. `_db` carve-out: `load_known_entities` + `update_idempotency_state`
    live in `_db.py` but are re-imported into the facade and called as bare globals.
    Import-direction is acyclic (facade → leaves; leaves → `_errors` sink only —
    never the facade). Patch targets at `scripts.wiki_skills.wiki_extract_concepts.<n>`
    are UNCHANGED by the split.
  - `prepare(args)` — argparse handler for `prepare` subcommand.
    **TASK 015 / R-015-3/4**: routes to `_batch_prepare` when `--batch`
    is given; otherwise opens one repo, calls `_load_known_and_drift`
    (known_concepts in the `--known-concepts-format` shape {`full`|
    `slugs-only`} + the source-independent `missing_concept_files` drift
    sweep), then `_recon_single`. The single-page envelope is unchanged.
    - `_load_known_and_drift(repo, vault, vault_root, fmt)` — loads
      known_concepts ONCE and sweeps concept-file drift ONCE (both are
      source-page-independent; a batch shares them across every entry,
      P-7). `slugs-only` reuses `known_slugs` (no second O(n) pass).
    - `_recon_single(source_page_arg, vault_root, vault_id, repo, known_out,
      missing_concept_files)` — PURE per-page recon (resolve via
      `_resolve_source_inside_sources` H-1; bounded read M-3+M-5; sha256;
      `check_idempotency`; relative `source_path` M-2). Returns the recon
      envelope dict OR an error dict (no `emit`, no `repo.close`). `prepare`
      maps an error dict → `emit(err, exit 2)`; `_batch_prepare` records it
      as a non-fatal per-entry result.
    - `_batch_prepare(args)` — reads a bounded JSON array of slugs
      (`INVALID_BATCH_FILE` exit 2 on bad shape/JSON); one repo +
      `_load_known_and_drift` once; per-slug `_recon_single(…,
      include_known=False)`; emits `{"known_concepts": […],
      "missing_concept_files": […], "batch": [entry, …]}` — known_concepts +
      drift hoisted to the top level ONCE (vdd-multi perf: per-entry embedding
      was O(N·|known|) stdout, re-inflating the P-6 payload). Per-entry error
      isolation (R-015-4d).
  - `apply(args)` — argparse handler for `apply` subcommand. **TASK 015 /
    R-015-5**: routes to `_batch_apply` when `--batch-candidates` is given;
    `--source-page`/`--source-hash` are argparse-optional (required only on
    the single-page path — enforced by a `MISSING_REQUIRED_ARG` guard).
    Loads candidates via `_load_candidates` (stdin bounded to cap+1 bytes;
    file path validated inside vault + rejected if non-regular
    (H-2 FIFO/device/socket guard) + read via `os.open(O_NOFOLLOW)` +
    bounded read), then validates BEFORE opening the DB and writes after.
    The Steps 2–5 body was factored (R-015-5d) into:
    - `_apply_validate(source_page_arg, source_hash_expected, vault_root,
      candidates)` — Steps 2–4 (resolve+read, `--source-hash` re-check per
      C-1 → `INVALID_SOURCE_HASH`/`SOURCE_CHANGED_DURING_EXTRACTION`, schema
      `_validate_candidates_schema` + `_preflight_sanitize` M-4). NO repo —
      so input-validation errors never touch the DB (the pre-015 ordering
      contract, guarded by the CWE-117 canary tests). Returns a validated
      dict or an error envelope with private `_exit_code`.
    - `_apply_write(validated, vault_id, vault_root, candidates,
      orchestrator_id, today, repo, known_slugs=None)` — Step 5 on an
      ALREADY-OPEN repo (caller-owned): M-9 warn, classify create/mention,
      write pages + upsert entities/refs + `build_manifest`. `known_slugs`:
      None → load fresh (single-page); a passed set → reuse + MUTATE in place
      (batch loads once, set grows as concepts are created so a later entry
      dedups without a re-scan — vdd-multi perf, O(E) once not O(N·E)).
      Returns `{_manifest, source_slug, source_hash, _exit_code}`.
    - `_apply_candidates_to_db(..., known_slugs=None)` — thin
      `_apply_validate`+`_apply_write` combiner used by the batch path
      (shared open repo + shared known_slugs set per entry).
    - `_batch_apply(args)` — one repo + one `load_known_entities` reused
      across all entries (`INVALID_BATCH_FILE` exit 2 on a >10 MiB / malformed
      file; per-entry `INVALID_BATCH_ENTRY`); per-entry catch tuple includes
      `sqlite3.Error` so a transient DB fault isolates to one entry instead of
      crashing the batch (vdd-multi HIGH); with `--ingest`,
      `dispatch_to_indexer(..., repo=repo)` once per entry on the shared
      connection (R-015-5f) — the per-entry dispatch ALSO catches
      `sqlite3.Error` → `DB_WRITE_FAILED` so a dispatch-phase DB fault isolates
      to one entry too; a failed `_try_update_idempotency_state` → per-entry
      `IDEMPOTENCY_UPDATE_FAILED`/`partial` (parity with single-page); emits
      `{"batch": […]}`.
    Single-page apply still dispatches via
    `_manifest_consumer.index_from_manifest` (now `repo=repo`, R-015-2);
    `_try_update_idempotency_state` (H-3: `sqlite3.OperationalError`-aware)
    emits `IDEMPOTENCY_UPDATE_FAILED` at exit 5. **F1 (dogfood 2026-06-01):**
    single-page apply now wraps its write+dispatch in `except sqlite3.Error` →
    a clean `DB_WRITE_FAILED` envelope (exit 5, retry-safe) instead of an
    uncaught `IntegrityError` traceback — the common trigger is applying to a
    source page that isn't in `pages` yet (run `wiki-reindex` first). Parity
    with the batch path's per-entry `sqlite3.Error` isolation.
  - `write_concept_page` — atomic tempfile + `os.replace`; symlink-refuse
    on target (Q15); content-hash skip via `os.open(O_NOFOLLOW)` for
    the existing-file read (M-5 closes symlink-follow disclosure);
    body construction uses `_sanitize_name` (regex allowlist) +
    `_sanitize_definition` + `_format_source_quote_block` (both
    delegate to `_sanitize_markdown_text` text-only allowlist — H-4
    replaces v3.0 denylist; escapes `&<>` + backticks + `[]` +
    leading-line markdown actives).
  - Helpers: `_validate_candidates_schema` (strict equality on keys +
    per-field caps + count bound + L-1 type-coverage on
    slug/source_span/entity_type + defensive isinstance guard);
    `_validate_source_hash` (C-1 argparse `type=`);
    `_validate_orchestrator_id` (H-8); `_path_is_absolute` (M-6
    cross-platform: catches `/etc/passwd` on Windows + `C:\foo` on
    POSIX); span regexes compiled with `re.ASCII` (L-2 — rejects
    Unicode digits); `_envelope_from_parse_error` builds CWE-117
    envelopes from `ExtractionParseError` structured attrs
    (`.error`/`.field`/`.reason`, NEVER `.value`/`.raw`).
  - Module-top imports of `validate_manifest`, `index_from_manifest`,
    `WikiIngestError` from `_manifest_consumer` lock `mock.patch`
    targets at `scripts.wiki_skills.wiki_extract_concepts.<symbol>`
    (PLAN.md Risk R-1, carried forward from v2).
  - **No `anthropic` import. No `ANTHROPIC_API_KEY`. No LLM call.**
  - Exit codes: 0 (success) / 1 (argparse) / 2 (`SOURCE_NOT_FOUND` |
    `INVALID_SOURCE_PATH` | `INVALID_SOURCE_SLUG` | `SOURCE_TOO_LARGE` |
    `SOURCE_CHANGED_DURING_EXTRACTION` | `INVALID_SOURCE_HASH` |
    `INVALID_CANDIDATES_PATH`) / 4 (`EXTRACTION_PARSE_ERROR` |
    `CANDIDATES_TOO_LARGE` | `CANDIDATE_COUNT_OUT_OF_BOUNDS` |
    `FIELD_TOO_LONG` | `UNKNOWN_FIELD` | `FIELD_QUOTE_NOT_IN_BODY` |
    `INVALID_SOURCE_SPAN`) / 5 (`PARTIAL_INDEX_FAILURE` |
    `IDEMPOTENCY_UPDATE_FAILED` — both preserve C-1 retry-safety) /
    6 (`MANIFEST_INVALID`). The v2 exit-3 `LLM_API_UNAVAILABLE` is
    retired.
  - **BREAKING CHANGE** vs v2: the monolithic `wiki-extract-concepts
    --vault X --source-page Y` (no subcommand) is rejected at
    argparse with a helpful pointer at `{prepare,apply}`.
- `wiki_import_article/` — **TASK 039/040 (ADR-007) the unified construct path** (CLI
  `wiki-import`; `wiki-import-article` is a back-compat alias). Decision-17 (no `import
  anthropic`; `prepare`/`apply`; one JSON envelope + stable exit). `prepare` shells to
  `html`/`pdf` (`_fetch.py` — output-dir mode, reader-first, X-login-wall + browser-UA PDF
  routing, `_skill_env` drops `PYTHONSAFEPATH`/`PYTHONPATH`, a hung fetch → typed
  `FETCH_FAILED(timeout)` with the temp dir reclaimed) → writes `_raw/<slug>.md` (+ optional
  `_attachments/`, folder-wide GC) and emits `known_concepts`/`existing_page_slugs`/`language`.
  The orchestrator REASON-summarizes; `apply` (`_authoring.py`) assembles the note, files
  concepts via `wiki-extract-concepts apply`, and indexes. **Invariants (2026-06 i18n +
  layout-matrix hardening, 14-round VDD):**
  - **i18n** — output language = vault `language` (`_vault_language`, en fallback, schemaless-safe);
    headings/labels via `NOTE_TEMPLATES` (en+ru); note-JSON fields are neutral `title`/`body`
    (legacy `title_ru`/`ru_body` accepted). NO hardcoded locale (`sanitize_name` `&`→space).
  - **collision keyspace** — `_mint_strategy(slug_strategy)`: mint in the layout's own keyspace
    (identity→preserve-unicode) so the guard compares like-for-like; footer entity links are
    `[[<minted-slug>|Name]]` (a fixed-point of every strategy → resolve on every layout).
  - **clickable `_raw`** — note links `[[_raw/<slug>]]`; `sources:` keeps the FS path.
  - **concept-gate** — `_layout_indexes_concepts` files `_concepts/` only on a layout that can
    index them (else summary-note-only; preserves Class A/B rebuildability).
  - **Decision-17 envelopes** — missing `--folder`/`--vault-root` → `INVALID_FOLDER`/
    `INVALID_VAULT_ROOT`; oversize note → `NOTE_TOO_LARGE` (bounded read); `main()` catch-all →
    `INTERNAL_ERROR` (type name only, CWE-209); `_imgtmp` reclaimed in a `finally`.
  Detect/contract: `_detect.py` (`--kind` auto-detect → REASON harness), `_context.py`
  (`known_concepts`/`existing_page_slugs` collision-guard inputs), `_errors.py` (exit codes).
  Full REASON contract: `skills/wiki-import/references/reason-contract.md`.
  - **TASK 044 — video sources:** `_fetch.py` adds a 3rd wrapped external skill
    (`transcript-fetcher`, `--transcript-bin`). `_video_host()` (pure URL-shape host class) routes
    `unambiguous_video` (youtube/vimeo/skool-lesson/x-broadcast+space) → `_fetch_transcript`;
    `ambiguous_x_status` + `--video` → `_fetch_x_status_with_video` (html+transcript **concat**,
    `engine=html+transcript:<origin>`); `not_video` + `--embedded-videos` → `_discover_embedded_videos`
    + append (ALWAYS-ON ad-exclusion chain: allowlist→ad-denylist→ad-context→ad-param→dedup→cap, every
    embed logged in envelope `embedded[]`). Exit map 7→DEP(6)/6→rate-limit/5→cookies/3→no-media;
    `--with-description`+`--lang` ALWAYS passed; `transcript_origin` falls back to `chosen_track_kind`
    (X-only field — S0). `_derive_slug` byte-caps the slug (≤180 B) — fixes `OSError 63` on titleless
    x-status sources. Off-by-default flags keep the not_video/default-x-status path byte-identical (NF-2).
- `_common.py` — `emit(payload, exit_code=0)` helper. **Error envelopes
  MUST pass `exit_code=6`** (convention from `wiki_init._emit`) so shell
  scripts and tests detect failure via `$?`.

## Contract

- Each module exposes `def main(argv: list[str] | None = None) -> int` for
  programmatic invocation by tests and the E2E harness.
- All output is JSON to stdout, one line; exit code 0 = success, 6 =
  validation/lookup error, 7 = interactive-confirm-required warning.
- CLI without a specific `--vault` (search/lint/reindex) passes the
  `_global_` sentinel (from `scripts/wiki_index/layout.py`) to the factory
  instead of inventing a fake vault_id.

## Read more

- `scripts/wiki_index/factory.py` — `make_repo` entry point
- `scripts/wiki_index/layout.py` — `GLOBAL_VAULT_SENTINEL` constant
- `scripts/wiki_index/layout_config.py` — **TASK 012** config-driven layout engine
  (the parsing grammar behind `--layout`); built-ins in `layouts/*.yaml`
- `scripts/migrate_known_issues_to_files.py` — **TASK 012 / PW-G** one-shot
  splitter (NOT a `wiki-*` skill): parses a monolithic `KNOWN_ISSUES.md` into
  per-issue Class-A `docs/issues/*.md` + a `.migration-report.md` flagging
  low-confidence (unknown-prefix / unusual-status / compound-prefix) issues.
  Run once per repo; the auto-render side then lives in `wiki-index-render
  --auto-indexes` + the PW-Q lint guard.
- `docs/PLAN.md` — Phase 3a task index
