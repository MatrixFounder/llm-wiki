# ★ THE NEGATIVE FIXTURE — and it is NOT optional

This transcript explicitly DEFERS: «отложили», «вернёмся к этому», «ничего не решать
без данных». There is no decision in it, and the correct extraction is `[]`.

`apply` returns `action: no_candidates`, **exit 0**.

Without this fixture, `CANDIDATE_COUNT_MIN = 0` is a constant no eval ever exercises —
and the anti-fabrication posture rests on a value nothing proves is reachable. This
fixture IS the mechanism from R-063-7, in fixture form: it proves that "no decisions"
is a reachable, REWARDED outcome.

The failure it guards against is concrete: if an empty extraction were a failure, the
model's cheapest path to a green run would be to invent a decision — and to invent the
quote that grounds it.
