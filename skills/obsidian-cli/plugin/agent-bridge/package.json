{
  "private": true,
  "description": "Dev-only build + type-check harness for the agent-bridge Obsidian plugin (TASK 070). Not published. main.js is GENERATED from main.ts by esbuild via `npm run build` (= scripts/build_agent_bridge.py --write) and hash-pinned by tests/test_agent_bridge_build_drift.py — never hand-edit it. Only manifest.json + main.js ship into a vault; this toolchain never does.",
  "scripts": {
    "build": "python3 ../../../../scripts/build_agent_bridge.py --write"
  },
  "devDependencies": {
    "esbuild": "0.28.1",
    "obsidian": "1.12.3",
    "typescript": "5.9.3"
  }
}
