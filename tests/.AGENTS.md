# tests/ — Testing Module Memory

**Conventions**

- Fixture data: `tests/fixtures/<kind>/<name>.{yaml,md,txt}`
  - `tests/fixtures/configs/` — config YAML fixtures for `config_loader` tests.
  - `tests/fixtures/vaults/` — minimal markdown vault layouts (added by
    task-001-10).
  - `tests/fixtures/source_extract/` — TASK 003 v3.1 fixtures: a synthetic
    source-page markdown + a canned candidates JSON (the operator-
    synthesised payload that v3.1's `apply` consumes). Used by
    `test_wiki_extract_concepts_integration.py` to exercise the full
    prepare+apply pipeline via subprocess against a real SQLite vault.
    No model-provider SDK is loaded; the v2 LLM call was deleted in
    bead 003-v3-06.
- Test files: `test_<feature>.py` (matches `pytest.ini::python_files`).
- Pytest fixtures live in `conftest.py` at the relevant level (root for shared,
  subdir for scoped).
- Each test must have a clear failure message (`assert x, "why x must hold"`).
- E2E tests use `tmp_path` for filesystem isolation; DB tests use sqlite3 over
  tmp_path-backed file (NOT `:memory:` — WAL pragma requires file).

**Anti-patterns to reject in review**

- Tests that pass by side-effect ("function ran without exception"). Always
  assert observable behavior.
- Mocked SQLite — use real sqlite3 over tmp_path. SQLite is fast enough; mocks
  hide schema mismatches.
- Shared mutable state between tests (no module-level DBs).
- Magic skip markers without rationale.
- `unittest.mock.patch("scripts.wiki_skills._manifest_consumer.<symbol>")`
  when the bound name lives in `scripts.wiki_skills.wiki_extract_concepts.*`
  or `scripts.wiki_skills.wiki_enrich.*` via module-top import. Patch the
  **importer's binding**, not the source-of-truth module. Risk R-2 in
  `docs/PLAN.md`; regression-tested by
  `test_patch_target_lock_at_skill_module` in `test_wiki_extract_concepts.py`.
- Calling out to any model-provider SDK in tests. The v3.1 skill does
  not import a model-provider SDK at all (Decision-17 — synthesis lives
  in the orchestrator). Tests must consume the strict candidates JSON
  contract directly (via canned fixtures or constructed dicts), not
  mock an SDK call. The v2 `mock.patch("provider.Client")` pattern was
  retired in bead 003-v3-11; do not reintroduce it.

**TASK 005 (Epic 7 entity resolution) test surface** — `test_schema_v3.py`
(PK/index/user_version v3), `test_reindex_is_candidate.py` + `test_reindex_alias_mirror.py`
(reindex round-trip + AM-3 canonicalization), `test_entity_resolution_dal.py`
(DAL, per-bead classes), `test_entity_resolution_cli.py` (wiki-confirm/alias/merge
via `main(argv)` + capsys), `test_wiki_search_alias_expansion.py`,
`test_wiki_lint_alias_collision.py`, `test_entity_resolution_durability.py`
(UC-14/15 §D8 round-trip — the binding acceptance gate),
`test_entity_resolution_envelope_safety.py` (CWE-117/209 — envelope never echoes
the alias surface), `test_extract_concepts_candidate_regression.py`, and
`test_vdd_multi_fixes.py` (the `/vdd-multi` F1–F6 fix regressions).
- **Ref-seeding convention**: `page_entity_refs` has a FK on `(vault_id, page_slug,
  page_project)`→`pages` but **none** on `entity_slug`. To seed refs without full
  page rows, insert directly under `PRAGMA foreign_keys = OFF` (see the `_seed_ref`
  helpers). DB durability tests delete the `.db` (+ `-wal`/`-shm`) and `reindex_full`
  to prove Class A→B reconstruction.

**TASK 007 (Epic 7 RAG, `wiki-query`) test surface** — `test_queries_subdir.py`
(`_queries` ∈ PAGE_SUBDIRS + discover_pages + type-fallback),
`test_reindex_cites.py` (R-6.5e `cites:`→`'cited'` union into the single
`replace_refs`, AM-3 ref_type-preserved, full **and** `--delta` symmetry),
`test_query_state_dal.py` (`check/record_query_state` idempotency DAL),
`test_wiki_query_prepare.py` (retrieval envelope + the **NL-question keyword-OR
regression** `test_natural_language_question_retrieves` — dogfood DF-Q1),
`test_wiki_query_apply.py` (grounding gate + `QUESTION_CHANGED` + sanitise +
Class A write), `test_wiki_query_index.py` (direct-DAL self-index/no-N+1,
`cited` backlinks, `query` log event + `--orchestrator-id` provenance, the
**>limit idempotency regression** `test_idempotency_holds_at_scale_above_limit`),
`test_wiki_query_durability.py` (UC-20 §D8 round-trip — the binding gate),
`test_wiki_query_e2e.py` (UC-16..21 over `main(argv)`),
`test_wiki_query_envelope_safety.py` (CWE-117/209 — never echoes the
question/answer/citation). Tests construct the synthesised answer + citations
directly (no model SDK — Decision-17, synthesis is the orchestrator's job).

**Coverage target**: `pytest-cov` ≥ 90% on `scripts/wiki_index/` and
`scripts/wiki_source/`. Enforced in benchmark/CI task (task-001-33).

**Read more**

- `pytest.ini` — top-level config
- `docs/tasks/task-001-10-pytest-fixtures.md` — pytest fixture spec (archived)
- `docs/tasks/task-003-v3.1-wiki-extract-concepts.md` — v3.1 testing
  contract (450 passed / 4 skipped target; envelope-shape parametrized
  test `test_apply_error_envelopes_never_echo_content` — the CWE-117
  invariant gate)
