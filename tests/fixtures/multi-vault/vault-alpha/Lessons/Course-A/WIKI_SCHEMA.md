---
name: WIKI_SCHEMA
schema_version: "1.1"
description: Course-local schema for Course-A inside vault-alpha. Two-tier promotion-spec.
---

# Course-A Schema (course-local)

Per promotion-spec §5.1, course-local schemas declare conventions specific
to one course. Cross-course concerns (promotion, shared layer) live in the
parent vault root `WIKI_SCHEMA.md`.
